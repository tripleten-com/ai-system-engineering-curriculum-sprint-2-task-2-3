{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "Coldline Task 2.3 public answer contract. This Task has no useful direct answer: the deliverable is a working repository, and the automated checks read PostgreSQL and the running application rather than a recorded claim. The mapping is required and must be empty, so an invented status or self-reported test result is rejected rather than accepted as evidence.",
  "title": "Coldline Task 2.3 answers",
  "description": "An intentionally empty answer mapping.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "answers"
  ],
  "properties": {
    "answers": {
      "description": "Empty by design. Add no field: the implementation is the evidence.",
      "type": "object",
      "additionalProperties": false,
      "maxProperties": 0
    }
  }
}
